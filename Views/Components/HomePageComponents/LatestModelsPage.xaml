<UserControl x:Class="OfflineCodingBot.Views.Components.HomePageComponents.LatestModelsPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Margin="10">
        <StackPanel>
            <TextBlock Text="Recently Opened Models" FontWeight="Bold" FontSize="20" Margin="0,0,0,15" />
            <ListBox ItemsSource="{Binding RecentModels}" SelectedItem="{Binding SelectedModel}" >
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <TextBlock Text="{Binding}" FontSize="14" TextTrimming="CharacterEllipsis" Width="400"/>
                            <Button Content="Load"
                                    Command="{Binding DataContext.LoadModelCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                    CommandParameter="{Binding}"
                                    Margin="10,0,0,0"
                                    Width="75" Height="25" />
                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </StackPanel>
    </Grid>
</UserControl>
